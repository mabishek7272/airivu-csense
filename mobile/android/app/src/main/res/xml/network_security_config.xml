<?xml version="1.0" encoding="utf-8"?>
<!-- Android blocks cleartext (plain HTTP) by default from API 28 - correctly, since a
     real deployment always serves over TLS (docs/10_PRODUCTION_DEPLOYMENT_GUIDE.md).
     This carve-out exists ONLY for the local dev stack, which - like every other client
     in this repo (customer-crm, developer-console, every e2e script) - talks to Traefik
     over plain HTTP on localhost; there is no cleartext exception for anything else, and
     a real deployment's API_BASE_URL (an https:// domain) never touches this block at
     all - domain-config allowlisting, not a blanket cleartextTrafficPermitted=true. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
