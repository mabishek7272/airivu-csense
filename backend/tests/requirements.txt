# The shared library is installed separately, not from here. A relative path in a
# requirements file resolves against the *working directory*, not the file, so `-e
# ../shared` worked when run from backend/ and failed everywhere else - including CI.
# Install it explicitly:  pip install ./backend/shared
pytest>=9.1.1
pytest-asyncio>=0.23
httpx>=0.27
psycopg[binary]>=3.1
minio>=7.2
psutil>=5.9
